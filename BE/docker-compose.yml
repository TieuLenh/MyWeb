version: "3.9"

services:
  backend:
    build: .
    container_name: spring_be
    env_file:
      - .env
    ports:
      - "8080:8080"

  frontend:
    build: ../FE
    container_name: react_fe
    ports:
      - "3000:3000"